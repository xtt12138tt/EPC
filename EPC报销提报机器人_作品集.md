# EPC 报销提报机器人

> 一个将非结构化签到表转换为标准 EPC 报销申请，并协助完成浏览器填报和收款人核验的 AI Agent 项目。

## Quick Facts

- **项目类型：** AI Agent / 流程自动化 / 内部效率工具
- **我的角色：** 独立完成业务拆解、规则设计、AI 解析、前端流程和浏览器自动化
- **核心技术：** Codex Skill、结构化 JSON、Flask、Playwright、SQLite、Edge 浏览器自动化
- **输入：** 粘贴表格、Excel、批量签到表
- **输出：** 标准 EPC 草稿、费用说明、收款人规则、EPC 页面填写结果
- **目标用户：** 需要处理 EPC 公账支付宝提报的业务同事

## Overview

EPC 报销提报原本需要人工阅读签到表、判断人员类型、拆分费用、编写费用说明、填写 EPC 页面，并逐一核对收款人。

这个项目把流程拆成三个协同模块：

```text
业务规则包
    ↓
AI/Codex 解析层
    ↓
标准化 EPC JSON
    ↓
前端审核层
    ↓
本地浏览器执行器
    ↓
EPC 页面和收款人核验
```

它不是一个“AI 直接替用户提交报销”的黑盒，而是一个可解释、可复核、保留人工确认节点的 AI Agent。

## Problem

### 原始资料不结构化

签到表可能来自 Excel、复制粘贴文本或多段表格，常见问题包括：

- 人员分段不固定；
- 姓名和手机号粘连；
- 玩家、接口人和兼职混在同一份表中；
- 金额列缺少清晰表头；
- 兼职场次费、餐补和交通补贴混在同一行；
- 代收、鸽子、未到场和补测信息写在备注中。

### EPC 类目不是简单的金额映射

同样是一笔钱，可能进入不同 EPC 类目：

- 玩家基础礼金 → 国内玩家礼金（常规）-基础礼金；
- 玩家交通补贴 → 国内玩家礼金（常规）-交通补贴；
- 接口人转介费 → 国内玩家礼金（常规）-转介费；
- 兼职场次费 → 国内兼职；
- 兼职餐补/交通补贴 → 其他。

### 自动化存在安全风险

如果没有中间审核，系统可能：

- 把不同人员类别混在一起；
- 重复计算转介费；
- 漏掉兼职补贴；
- 删除 EPC 中实际需要保留的收款人；
- 在用户没有确认时提交申请。

## Goal

项目目标不是单纯减少点击，而是建立一条可复核的自动化链路：

1. 将原始签到表转换为结构化数据；
2. 将业务规则固化为可复用 Skill；
3. 将费用、收款人和总额统一校验；
4. 让 AI 解析和浏览器填报使用同一份 JSON；
5. 在高风险节点保留人工确认；
6. 支持单笔和批量任务。

## Solution

### 1. 业务规则作为可维护的 Skill

没有把所有判断逻辑硬编码在 Prompt 中，而是拆出可维护的规则包：

- `SKILL.md`：工作流和安全边界；
- `business-rules.md`：人员分类、费用映射和排除规则；
- `rate-table.md`：玩家礼金费率和反推校验；
- `expense-note-template.md`：费用说明统一格式；
- `batch-mode.md`：批量解析协议。

这样做的好处是：业务规则可以独立更新，不需要每次修改浏览器自动化代码。

### 2. 统一标准 JSON

AI/Codex 不直接操作 EPC，而是先输出标准化数据：

```json
{
  "project_id": "脱敏项目号",
  "gift_common": {
    "total_sample": 18,
    "rows": []
  },
  "parttime": {
    "rows": []
  },
  "other": {
    "rows": []
  },
  "payee_rules": {
    "known_players": [],
    "specific": [],
    "proxies": []
  },
  "expense_note": "统一格式费用说明",
  "grand_total": 0,
  "warnings": [],
  "missing": []
}
```

前端负责展示和编辑这份数据，填报脚本也读取这份数据。这样避免了“AI 解析一份、前端重新计算一份、脚本再猜一份”的不一致问题。

### 3. 人员和费用的规则化拆分

#### 玩家

当资料明确写出基础礼金和交通补贴时：

```text
玩家实际收款 = 基础礼金 + 交通补贴
```

#### 接口人

接口人记录同时保留：

- 姓名；
- 收款类型；
- 应收金额；
- 转介人数；
- 手机号。

转介费只进入一次，避免和接口人个人金额重复计入。

#### 兼职

兼职场次费和补贴采用双口径：

- 第 1 页“国内兼职”填写工作报酬；
- 第 1 页“其他”填写餐补/交通补贴；
- 第 2 页收款人填写实际应收总额。

费用说明单位也按工作类型区分：

- 测试执行：场 / 场次单价；
- 电访、甄别、街访、海捞、其他测试支持：个样本 / 样本单价。

### 4. 5000 前端审核层

前端网页承担“人机协同控制台”的角色：

- 显示标准化草稿；
- 允许用户修改字段；
- 展示 warnings、missing、assumptions；
- 阻止存在阻断错误的单据进入 EPC；
- 提供批次、状态、日志、截图和核验结果。

### 5. Playwright 浏览器执行器

本地执行器连接使用者自己的 Edge 登录态，自动完成：

1. 打开 EPC；
2. 填写发包序列、发包类型、项目和成本归属；
3. 填写费用说明和报销明细；
4. 上传产品确认截图；
5. 读取 EPC 收款人名单；
6. 匹配姓名和手机号；
7. 填写类型和金额；
8. 回读金额、支付宝登录号和实名信息；
9. 对异常进行结构化展示。

## Key Design Decisions

### 决策 1：AI 只解析，不直接提交

AI 的职责是理解资料并生成 JSON。浏览器自动化必须读取确认后的 JSON，避免模型直接操作高风险页面。

### 决策 2：显式金额优先于标准价

价目表只用于补全缺失字段和校验，不覆盖原始资料中明确出现的个人金额、数量和小计。

### 决策 3：把不确定性显式化

无法确定时输出：

- `warnings`：需要用户知悉的风险；
- `missing`：缺失的必要字段；
- `assumptions`：使用过的推断；
- `error`：解析失败原因。

### 决策 4：最终提交保持人工闸门

自动化可以填写和核验，但不会在没有当前对话明确确认的情况下点击最终提交。

## Batch Design

批量流程把原始资料先写入 `input.json`，每张单独立解析，再由协调器统一写入 `codex_draft.json`。

批量设计的关键约束：

- 每张单有独立 `order_id`；
- 单据之间不能共享人员和金额推断；
- 单张解析失败不能阻塞其他单；
- 共享草稿文件只能由协调器写入；
- 解析过程可写进度，但正式 JSON 需要一次性完整更新。

## Results

本项目目前形成了可运行的端到端流程：

- 支持单笔和批量提报；
- 支持粘贴表格、Excel 和标准 JSON；
- 支持玩家、接口人、兼职、补贴、代收和排除人员处理；
- 支持费用明细和收款人双重对账；
- 支持第 1 页截图确认和第 2 页收款人核验；
- 支持暂停、继续、重试和服务重启后的恢复提示；
- 已整理成可分发的业务规则包、5000 前端服务包和本地执行器包。

这里不虚构节省比例或金额准确率；项目效果主要体现在流程标准化、风险可见化和可重复执行。

## Limitations

- EPC 页面和下拉选项可能发生改版，需要维护选择器和业务映射；
- 兼职补贴如果缺少表头或单价，仍需要人工确认；
- 当前前端和本地执行器依赖同一运行目录；
- EPC 登录态必须由每个使用者自己维护；
- 还没有接入正式的企业级用户权限、审计和集中部署。

## What I Learned

1. AI 项目最重要的不是让模型“自由发挥”，而是建立清晰的输入、输出和校验契约。
2. 业务规则应当独立成可维护文档和 Skill，而不是散落在多个 Prompt 和脚本中。
3. 浏览器自动化的难点不只是定位元素，更是处理登录、人工确认、异常恢复和数据一致性。
4. 费用、人员和页面状态都需要可回读、可解释、可人工修正。
5. 面向真实业务时，安全边界和人工闸门与自动化效率同样重要。

## Visual Asset List

发布时建议准备以下脱敏素材：

- 系统整体架构图；
- 原始签到表与标准 JSON 对照图；
- 5000 前端草稿审核页面截图；
- EPC 第 1 页填写前后截图；
- 收款人差异核验页面截图；
- 批量状态面板截图；
- 费用说明模板示例。

所有截图必须遮盖项目号、姓名、手机号、预算负责人、内部域名和任何登录信息。

