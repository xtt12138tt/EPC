# 脱敏交互 Demo 运行时

本目录是作品集网站中 iframe 使用的真实 5000 前端脱敏副本。

## 启动

推荐直接在上级目录双击：

```text
启动交互作品集Demo.bat
```

它会：

1. 写入虚构项目、人员和金额；
2. 启动本地 Flask Demo；
3. 打开作品集页面；
4. 在作品集页面中嵌入 `http://127.0.0.1:5050/batch`。

## 安全

演示数据均为虚构数据，不连接真实 EPC；不要把真实 `data`、截图、数据库、Cookie 或密钥放入本目录。
